/**
 * Safar AI - Route Lookup Service (Stage 1 Stub)
 * Waypoint lookup will be implemented in subsequent stage.
 */

export function getRoute(params) {
  return {
    status: "stub",
    message: "Route service foundation ready. Lookup logic will be active in next stage."
  };
}
