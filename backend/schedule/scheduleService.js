/**
 * Safar AI - Schedule Lookup Service (Stage 1 Stub)
 * Static timetable lookup will be implemented in subsequent stage.
 */

export function getSchedule(params) {
  return {
    status: "stub",
    message: "Schedule service foundation ready. Lookup logic will be active in next stage."
  };
}
